# Demo Bot IVR Requirements
# Clean, minimal dependencies for Sarvam STT/TTS + Strands + Ozonetel

# Core web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# WebSocket support
websockets==12.0

# Environment and configuration
python-dotenv==1.0.0

# HTTP requests for Ozonetel API
requests==2.31.0

# AWS SDK for Bedrock
boto3==1.34.0
botocore==1.34.0

# Strands agent framework (if available)
# strands>=0.1.0  # Uncomment if strands is available

# Audio processing
# No heavy audio libraries needed - using raw PCM

# Logging and utilities
python-json-logger==2.0.7

# Development dependencies (optional)
pytest==7.4.3
pytest-asyncio==0.21.1